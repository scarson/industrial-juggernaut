# Session Handoff — Industrial Juggernaut — 2026-05-27

> Checkpoint of a long autonomous session that went from design docs → a complete M1 rules engine → a stronger-agent (MCTS) build → a balance-sweep harness. The session is NOT ending; this is a durable checkpoint. Points at the canonical artifacts (specs/plans/reports) rather than duplicating them.

## Headline state
- **Branch:** `claude/document-game-design-VpqqB` (all work; the only dev branch).
- **Tip:** `59e18fa` (pushed). `origin/main` is at `72944bb` (PR #10). The branch is ahead of main by the post-PR-#10 sweep work (S1–S5 + plan/claim/handoff commits).
- **Tests:** 374 passing, strict `tsc` clean.
- **RESOLVED — the game IS balanceable.** S5's wide grid found "no healthy config" (`docs/sweeps/2026-05-27-balance-report.md`), but the follow-up 600-game calibration (`docs/sweeps/2026-05-27-calibration-report.md`, via `src/sweep/calibrate.ts`) proved that was a **measurement artifact**: `boardSize=96, radius=2, ironCount=12, victoryThreshold=12` PASSES all 7 criteria at 600 games (the same cell failed S5 only on seatBias 0.233 at 150 games; at 600 games seatBias=0.167, within noise). The decisive knob is **base control radius 5 → 2**. Full reasoning + the deferred rules-redesign options: `2026-05-27-balance-rules-analysis.md`. **Next: re-validate the config under MCTS (weak-agent caveat) before adopting it as `defaultConfig`.**
- **PRs:** #2,#5,#6,#7,#8,#9,#10 all merged to `main` with **normal merge commits** (NOT squash — see guardrails). No PR currently open. The sweep work (S1–S5) is NOT yet PR'd to main.

## Canonical artifacts (the real source of truth — read these, don't trust this doc's summary over them)
- **Design docs (root):** `2026-05-18-design-critique.md`, `2026-05-18-code-representation-options.md` (all-TS-on-Cloudflare decision), `2026-05-27-agent-roadmap.md` (Part 2 = the strong-agent vision).
- **Rules:** `industrial-juggernaut-rules-v10.md` (reconciled this session: per-player factory-death clock; base-triangle rule clarified — see below).
- **Specs:** `docs/superpowers/specs/2026-05-27-rules-engine-m1-design.md`, `…-stronger-agent-mcts-design.md`, `…-learned-agent-alphazero-design.md`, `…-balance-sweep-harness-design.md`.
- **Plans (Living Documents — Execution Status banners are kept current):** `docs/plans/2026-05-27-rules-engine-m1-plan.md` (✅ done), `…-stronger-agent-mcts-plan.md` (⏸ paused at A5.1), `…-learned-agent-alphazero-plan.md` (⏸ gated), `…-balance-sweep-harness-plan.md` (S1–S4 ✅, S5 🚧).
- **Pitfalls:** `docs/pitfalls/{implementation,testing}-pitfalls.md` (GEO-1..6; GEO-6 = per-player factory clock).

## What shipped this session (pointers)
- **M1 rules engine — COMPLETE & on main** (PRs #2–#7): pure TS engine — geometry (cube/hexline/hull/sightline), board gen + iron-CSP, territory `control`, build/combat/`applyAction`/stranded/status/legalActions/turn, greedy archetype agent, driver + 1000-game acceptance. Plan: M1 plan (all phases ✅).
- **Stronger-agent (heuristic MCTS) — A1–A5.1 on main** (PR #10): perimeter-aware `evaluate` + `samplePolicy` (A1); MCTS core — max^n tree, PUCT, progressive-widening, combat chance nodes, determinized turn order, shared `stepRound`, `runMcts` (A3); `chooseActionMCTS` + IS-MCTS legality filtering (A4); Elo arena (A5.1).
- **Two root-cause engine bug fixes** (on main): (a) base placement illegal at `basesInHand===0` (legalActions self-consistency); (b) **THE big one — the 2nd base was impossible** (`isLegalBasePlacement` required a 2-base triangle from a 1-base start → no growth → factory-death). Fixed: triangle rule applies only to the perimeter-establishing 4th+ base. This was the true root cause of the whole "games degenerate" saga.
- **Authorized balance tuning** (on main): factory-death clock changed shared→per-player (threshold 8), GEO-6, rules doc reconciled. (Now known to be a *symptom-level* fix relative to the base-placement bug.)
- **Designs + plans** (on main): stronger-agent MCTS + learned-agent (AlphaZero) + balance-sweep — all specced, plans `plan-review-cycle`'d.
- **Balance-sweep harness S1–S4** (on branch, post-#10): metrics (`8e104fc`), health gate+rank (`c81f5fd`), runner w/ CRN+CIs (`5148523`), orchestrator+report+infeasibility-guard (`53de15b`).

## S5 — COMPLETE (committed `59e18fa`, pushed)
- **What ran:** `src/sweep/main.ts` — wide geometry grid (`boardSize {96,150,220,300} × radius {2,3,4,5} × {ironCount,victoryThreshold} endpoints` = 64 cells), 150 games/config with CRN, then OFAT of the critique's balance variables (autoWinAt6, killBounty, victoryThreshold, attackRange) around the best near-miss. Report at `docs/sweeps/2026-05-27-balance-report.md`; outcome in the sweep plan's Discoveries.
- **Outcome:** **0/64 configs pass all 7 health criteria** — BUT the misses cluster on the two *softest* criteria (seatBias near its noise floor; medianTurns off-by-one), and two configs miss by exactly ONE criterion. This is a "needs calibration before concluding," not a clean "unbalanceable." Full nuance in the next section — DO NOT act on the bare "0/64" headline.

## THE central open question — ANSWERED: YES, the game is balanceable (parameters alone)
**Question:** *Is the game balanceable into multi-turn strategic depth?* **Answer (2026-05-27, after the 600-game calibration): YES — without any rules-mechanic change.** `boardSize=96, radius=2, ironCount=12, victoryThreshold=12` passes all 7 health criteria at 600 games (median 3, setupDecided 0, ironVictory 0.79, capHit 0.017, seatBias 0.167, leadVolatility 0.35). The decisive parameter is **base control radius 5 → 2** (a smaller disk → iron acquisition is no longer a one-move uncontested land-grab — the mechanism the rules-analysis P1/P3 identified, fixed by tuning not redesign). It is a **marginal, narrow** pass (only healthy cell of 16; vt12 is knife-edged: vt11→median 2, vt13→unwinnable) and under the greedy agent, so **re-validate under MCTS before adopting**. The rules-redesign options (perimeter-gated iron; hold-iron-for-N-rounds victory model) are now PARKED, not discarded — see `2026-05-27-balance-rules-analysis.md` §0. The history below explains why the crossroads existed; it is kept for the record.

### History — why this looked like a crossroads (S5, now superseded)

**What S5 actually found (the nuance that matters):**
- **0/64 configs pass all 7 criteria.** The setup-decided degeneracy is *gone* almost everywhere — `setupDecidedFraction = 0` for 56/64 cells (the base-placement fix worked). The two binding failure modes are now: **`medianTurns` too short** (games end turn 1–2 across most of the grid — the radius-N base still blankets enough iron to end fast) and **`seatWinBias > 0.20`** (fails in nearly every cell).
- **Confound #1 — seatBias is at its statistical noise floor.** seatBias values quantize to 0.167 / 0.233 / 0.333 / 0.417 / 0.5 — i.e. multiples of ~1/6 of a ~30-game per-seat-count bucket (150 games ÷ the 2–6P rotation). At ~30 games/seat-count the per-seat win-rate CI is ≈ ±0.18, so a 0.20 cap is *barely above noise*; many "seatBias FAIL"s are plausibly sampling artifacts, not real first-mover advantage. **Until games/config is raised enough to separate signal from noise, seatBias failures are not trustworthy.**
- **Confound #2 — two configs miss by exactly ONE criterion**, and each missed criterion is one of the two soft ones:
  - `boardSize=96, radius=2, ironCount=12, victoryThreshold=12`: passes 6/7 (median 3, setupDecided 0, ironVic 0.78, leadVol 0.31) — **fails only seatBias 0.233** (likely noise per confound #1).
  - `boardSize=96, radius=2, ironCount=16, victoryThreshold=12`: passes 6/7 — seatBias 0.167 **passes** — **fails only medianTurns 2** (one turn under the min-3 "has depth" band).
- **OFAT around the near-miss (directional, weak-agent caveat):** `victoryThreshold` is the strongest length lever (10→median 2, 12→3, 14→18 but ironVic collapses to 0 — i.e. 14 is too high to reach). `killBounty` full→none drops ironVic 0.78→0.68 (mild). `autoWinAt6` on/off barely moves anything. `attackRange` 5 vs 6 mildly affects ironVic.

**Why this is a crossroads, not an autonomous fix.** The two binding criteria are *definitional game-design judgments*, not bugs: (a) is "median 2 turns" actually too short, or is the min-3 "has depth" band miscalibrated? (b) is the 0.20 seatBias cap meaningful at 150 games, or must games/config rise (≈600+ to halve the CI) before seatBias is even measurable? Both questions are Sam's to answer. Three honest paths, in increasing cost:
1. **Calibrated re-run (cheapest, recommended first):** raise games/config (≈600+) to pull seatBias out of the noise floor, and narrow the grid around the `b96/r2/vt12` near-misses (the only region producing median-3 multi-turn games with low setupDecided). If a config then passes all 7, the game *is* balanceable and we have it. **This must happen before any "unbalanceable" conclusion — the current run cannot distinguish "no healthy config" from "under-powered measurement."** (Do the queued progress-logging FIRST so the longer run is observable.)
2. **Threshold reconsideration (Sam-gated):** if Sam judges "median 2 turns" acceptable or the seatBias cap too strict at achievable sample sizes, the near-miss `b96/r2/iron16/vt12` is already a defensible balanced baseline. This is loosening a *definition*, not loosening a gate to manufacture a pass — but it's Sam's call, not the agent's.
3. **Redesign-level finding (only if #1 fails):** if a calibrated, narrowed re-run still finds nothing, *that* is the genuine "the geometry/economy can't produce balanced multi-turn games" verdict — surface it to Sam as a rules-level redesign question (the radius-N base blanketing iron is the mechanism; it ends games before strategy develops).

**Prior context (still true):** the base-placement fix made the game *playable* but games are decided fast (a radius-5 base on a ~93-hex board blankets ≥10 of 14 iron; pre-fix, 48/200 games won AT SETUP). No single hand-picked knob fixed it (validated) — which is why the sweep exists. S5 shows the *setup* degeneracy is fixed but a *turn-1-end* degeneracy remains in most of the grid.

## Deferred items (each: unblock condition + link)
- **MCTS trustworthiness gates A5.2 (robustness + exploiter) and A6 (the 4 gates).** ⏸ DEFERRED pending a **balanced game config** (so games have depth for lookahead to matter — currently MCTS@60 *loses* to greedy on the 1-turn game, and greedy self-destructs, making gate 2 unassessable). Unblocker: a balanced config from S5's follow-up (S5 itself found none clean — see central question; path 1/2 produces one) → adopt as `defaultConfig` (human-gated balance decision) → resume. See `docs/plans/2026-05-27-stronger-agent-mcts-plan.md` Overall banner + Discoveries.
- **Learned (AlphaZero) agent — entire milestone.** ⏸ DEFERRED pending (1) the MCTS gates resuming AND (2) evidence MCTS is *insufficient* for trustworthy sweeps (it may suffice → YAGNI exit). Double-gated behind the balanced config. See `docs/plans/2026-05-27-learned-agent-alphazero-plan.md` top banner.

## Seams (where context is silently lost)
1. ~~S5 (running) → its plan Discoveries merge race~~ — RESOLVED (S5 done; plan + report committed at `59e18fa`). No live process owns any sweep file now.
2. **A balanced config → MCTS gate resumption.** S5 yielded NO clean healthy config (see central question). When a balanced config IS established (via path 1/2 after Sam's direction), adopting it as `defaultConfig` is a SEPARATE step (a balance decision Sam authorized in spirit, but a real config change). Adoption will shift MANY test distributions (the 1000-game acceptance, etc.) — expect to update tests that encoded the old (degenerate) balance to the new behavior (correct-not-loosen). Only after adoption do A5.2/A6 become meaningful.
3. **Greedy baseline self-destructs (current balance).** Gate 2 ("MCTS beats greedy") is currently confounded: greedy walks into the factory-death self-destruct MCTS avoids. After the balanced config, re-assess whether MCTS genuinely beats a *competent* greedy — that result drives the learned-agent gate.
4. **Branch vs main divergence pattern.** After each PR merges, this session does `git reset --hard origin/main` then force-push-with-lease the branch on the NEXT push (the branch's pre-squash... no — we used merge commits; the branch fast-forwards). Current: branch ahead of main by sweep S1–S4. Next PR merges those.

## Operational guardrails accumulated this session (so a fresh agent doesn't re-learn them)
- **Merge with `--merge` (normal merge commit), NEVER squash** (Sam's instruction; matches `docs/git-strategy.md`). Preserve per-commit history.
- **All work on `claude/document-game-design-VpqqB`.** Commit + push after EVERY task (the container is ephemeral — unpushed work is lost; the stop-hook nags about it). PR + merge at phase boundaries; `git fetch origin main && git reset --hard origin/main` to resync after a merge.
- **Subagent-driven execution:** one fresh subagent per task; brief it self-contained (it has none of this context); verify its work (trust-but-verify — several left work uncommitted or hit container restarts; check `git status`/`git log` and the suite yourself); commit/push; update the plan banner. Don't dispatch two implementer subagents at once (branch race).
- **5-option/adversarial pattern for any meaningful decision** (Sam's standing instruction) — done for: candidate-action repr (MCTS), learned-agent stack/value/encoding/policy/loop, factory-clock fix, base-placement semantics, provisional-balance lever, config-health metric.
- **Work autonomously; don't prompt** — BUT surface genuine design crossroads (did so for the balance/1-turn discovery, which Sam decided as "A-to-unblock-B").
- **Board-coordinate discovery:** the seed-1n/size-96 board is a 93-hex asymmetric oval; off-board coords exist (`(0,5,-5)`,`(8,-8,0)` off; `(0,0,0)`,`(2,-2,0)`,`(4,-4,0)`,`(5,-5,0)`,`(6,-6,0)`,`(0,4,-4)` on). Use on-board coords or `mkState` unioning in fixtures.
- **Iron-CSP feasibility:** small boards can't hold much iron (max-degree-1 spacing); the sweep guards infeasible `(boardSize,ironCount)` combos.
- **`ironOverTime[0]` is post-turn-1, not setup;** `setupDecided` is computed by mirroring `runGame`'s board (post-`generateBoard` rng → `setupGame`).
- **Engine determinism:** all randomness via the seeded PCG (`src/rng/pcg.ts`); no `Math.random`. Strict tsconfig; never relax it. Hex Sets/Maps keyed by `key()` string (GEO-4); derived state recomputed not cached (GEO-5).

## Queued improvements (small, do before the next long run)
- **Sweep progress logging (Sam-requested) — NOW UNBLOCKED (S5 finished; `main.ts` is free to edit).** The S5 run was *silent* (`main.ts` printed only the "Stage 1" header, then ran ~64 configs at 100% CPU with no per-config output), which made "stalled vs. slow" undiagnosable without tailing the process. Add an optional `onProgress(done, total, label, metrics)` callback to `sweepGrid`/`runConfig` (backward-compatible default no-op; test it), and have `main.ts` log per-config progress (`config k/N: bs… r… → medianTurns/health`) + a per-stage heartbeat. **Do this BEFORE any calibrated re-run** (a 600-game/config re-run is much longer than S5 and must be observable).

## Priority queue (numbered, with dependencies)
1. ✅ **S5 complete** — report `59e18fa`, pushed.
2. ✅ **Progress logging shipped** (`5d84f40`) + **calibrate.ts** (`c7ec0cb`) + **calibration report** (`a911990`) — the 600-game re-run found a healthy config (see headline / central question). Rules-level analysis doc `0c2ec42`.
3. **Re-validate `b96/r2/iron12/vt12` under MCTS** (the weak-agent caveat / P6). Cheap, and it both (a) guards against adopting a config that's only healthy under greedy play, and (b) is the first half of the paused MCTS trustworthiness work. **Immediate next action.**
4. **(if #3 holds) Adopt `b96/r2/iron12/vt12` as `defaultConfig`** — Sam-gated (it's a real balance decision: radius 5→2 changes the game's feel; iron 14→12; threshold 10→12). Adoption shifts MANY test distributions (the 1000-game acceptance, etc.) — update tests that encoded the old degenerate balance to the new behavior (correct-not-loosen).
   - **(if #3 does NOT hold under strong play)** the rules-redesign options in `2026-05-27-balance-rules-analysis.md` (chiefly P3: gate victory-iron behind a committed perimeter, not a radiating disk) become the live path — a Sam-gated design cycle, not an autonomous fix.
5. **(after the config is adopted)** Resume the rest of the MCTS gates: A5.2 (robustness + exploiter), then A6 (the 4 trustworthiness gates) — now meaningful on a balanced game.
6. **(after #5)** Learned-agent gate decision: if A6 shows heuristic-MCTS *insufficient* → execute the learned-agent plan; else → YAGNI-shelve it (the intended exit).
7. **PR+merge** the sweep work (S1–S5 + progress logging + calibrate.ts + reports + analysis doc) to main with a normal merge commit (NOT squash).

## Continuation prompt (paste-ready for a fresh agent)
> You are continuing an autonomous build of Industrial Juggernaut on branch `claude/document-game-design-VpqqB`. Read `docs/handoffs/2026-05-27-session-handoff.md` first (especially "THE central open question"), then the four plans under `docs/plans/` and the S5 report `docs/sweeps/2026-05-27-balance-report.md`. **S5 (the full balance search) is COMPLETE:** it found NO config passing all 7 health criteria in the 64-cell grid, BUT the failures cluster on two soft criteria (seatBias at its statistical noise floor at 150 games/config; medianTurns off-by-one), and two configs miss by exactly one criterion (`b96/r2/iron12/vt12` fails only seatBias 0.233 ≈ noise; `b96/r2/iron16/vt12` fails only medianTurns 2). This is a CALIBRATION CROSSROADS for Sam, not a clean "unbalanceable" verdict — do NOT autonomously conclude redesign, loosen thresholds, or change `defaultConfig`. The immediate next action is to surface this crossroads to Sam and get direction (default recommendation: path 1 = a calibrated re-run at ≈600 games/config narrowed around `b96/r2/vt12`, since the current run can't distinguish "no healthy config" from "under-powered seatBias measurement"). Independently and FIRST: implement the queued sweep progress logging (see "Queued improvements") so any re-run is observable, then PR+merge the sweep (S1–S5 + logging) to main. Operate per the guardrails (merge-commits-not-squash, commit+push per task, subagent-driven with verification, 5-option-adversarial for meaningful decisions, work autonomously but surface genuine design crossroads). Do NOT loosen any health gate or trustworthiness gate to manufacture a pass — a failing gate is a finding.

## Adversarial review log (handoff skill Phase 4)
- **Round 1 (naive fresh agent):** added the "canonical artifacts" + "read these, don't trust the summary" framing and glossary-ish guardrails so a cold agent can orient; spelled out the double-gating of the learned agent. (2 fixes)
- **Round 2 (recency-bias audit):** pulled forward mid-session items that recent work overshadowed — the two engine bug fixes, the factory-clock authorization, the merge-commit rule, the board-coordinate discovery. (4 fixes)
- **Round 3 (seam auditor):** documented the 4 seams explicitly (S5↔plan-Discoveries merge race; recommended-config↔adoption↔gate-resume; greedy-self-destruct confounding gate 2; branch/main divergence). (1 new seam added vs first draft)
- **Round 4 (operational guardrails):** confirmed all session guardrails are in the handoff AND that the durable ones (factory clock GEO-6, base rule, merge policy) live in pitfalls/rules/git-strategy, not only here. (verified)
- **Round 5 (loss-averse):** scanned for "worth capturing later" items — the iron-CSP feasibility + `ironOverTime[0]`-is-post-turn-1 findings are in the sweep plan Discoveries AND summarized here. (verified)
- **Round 6 — session-specific: "balance-confound auditor."** This session's defining character is that *agent quality and game balance are entangled* — repeated findings (greedy self-destructs, MCTS loses on 1-turn games, gate 2 unassessable) all trace to unbalanced game state, and a fresh agent could wrongly conclude "MCTS is useless" or "the agent is broken" when the real cause is balance. Added explicit framing in "THE central open question" + seam #3 + priority #2 so the next agent does NOT misattribute the gate-2 result to agent weakness before the balanced config exists. (2 fixes)
- **Final pass:** re-ran rounds 1–6; zero further material findings.
- **Post-S5 update (Sam-requested):** resolved "THE central open question" with the actual S5 result. The load-bearing edit was framing 0/64 as a *calibration crossroads*, not a redesign verdict — guarding the next agent against the obvious-but-wrong "the game is unbalanceable" conclusion, since the binding failures are seatBias-at-noise-floor and medianTurns-off-by-one (both confounds, not proofs). Captured the two single-criterion near-misses, the OFAT directional findings, the three paths with path-1-first recommendation, and refreshed the headline / S5-section / seams / deferred-items / priority-queue / continuation-prompt to match. Did NOT change thresholds, `defaultConfig`, or the gate — those are Sam's call.
