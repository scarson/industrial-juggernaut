# MCTS Variants — Quick Comparison @50 iterations

**Date:** 2026-05-29. **Trigger:** Sam asked which MCTS fix approaches play "more like Opus." Each variant tests one architectural tweak on top of the @50 baseline (which gets 0% vs heuristic).

**Reference:**
- heuristic = baseline strong (target to match or beat).
- lookahead2 = 80.7% vs heuristic on (c) 2P. The "ceiling" we're trying to approach.
- MCTS@50 baseline = 0% (per A1-era earlier data).

**Methodology:** 16 2P games per variant, variant (c), baseSeed 36000.

## Results (sorted by win rate)

| Variant | Win rate | Δ vs baseline | Description |
| --- | ---: | ---: | --- |
| baseline | 0.0% | +0.0pp | Default @50: PW, T=1, cPuct default, maxDepth 8 |
| fixed-candidates | 0.0% | +0.0pp | @50 + candidateMode=fixed (greedy action always included) |
| temp-0.1 | 0.0% | +0.0pp | @50 + PW temperature=0.1 (samples close to argmax) |
| temp-0.01 | 0.0% | +0.0pp | @50 + PW temperature=0.01 (near-pure-argmax sampling) |
| depth-2 | 0.0% | +0.0pp | @50 + maxDepth=2 (matches Opus's 2-ply structure) |
| depth-2+fixed | 0.0% | +0.0pp | @50 + maxDepth=2 + candidateMode=fixed |
| low-cpuct | 0.0% | +0.0pp | @50 + cPuct=0.5 (less exploration, more exploitation) |
| prng-aware | 0.0% | +0.0pp | @50 + PRNG-aware leaf eval (peeks T2 turn-order draw, weight=5) |
| iron-share | 0.0% | +0.0pp | @50 + iron-share leaf eval (tabletop-valid 'who probably goes first') |
| prng-aware+fixed | 0.0% | +0.0pp | @50 + PRNG-aware + candidateMode=fixed (combo) |
| prng-aware-strong | 0.0% | +0.0pp | @50 + PRNG-aware with weight=20 (stronger turn-order bias) |

## Interpretation

No variant shows clear improvement >10pp over baseline. May need code-level fixes (PRNG-aware leaf eval, broader PW candidate diversity beyond config knobs).

At n=16 per variant, 95% CI on a fair coin is ~±25pp, so differences of <10pp are within noise. The best variants should be re-tested at higher n (50+) to confirm.

---
*Generated by `src/sweep/mcts-variants-quick.ts`.*
